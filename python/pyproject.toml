[build-system]
requires = ["maturin>=1.4,<2.0"]
build-backend = "maturin"

[project]
name = "goldy"
version = "0.1.0"
description = "Python bindings for the Goldy GPU library"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.9"
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Rust",
    "Topic :: Multimedia :: Graphics",
    "Topic :: Scientific/Engineering :: Visualization",
]
keywords = ["gpu", "graphics", "vulkan", "rendering", "compute"]
authors = [
    { name = "Mohamed Koubaa" }
]
dependencies = [
    "numpy>=1.20",
]

[project.urls]
Homepage = "https://github.com/koubaa/goldy"
Repository = "https://github.com/koubaa/goldy"
Documentation = "https://github.com/koubaa/goldy#readme"

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pillow>=9.0",
]

[tool.maturin]
features = ["pyo3/extension-module"]
python-source = "python"
module-name = "goldy._goldy"

# Include Slang libraries in the wheel (platform-specific)
# These are copied to the package directory by build-slang.py before building
include = [
    { path = "python/goldy/*.dll", format = "wheel" },
    { path = "python/goldy/*.so", format = "wheel" },
    { path = "python/goldy/*.dylib", format = "wheel" },
]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "gpu: tests that require GPU access (may use software renderer in CI)",
]

