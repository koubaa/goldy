[build-system]
requires = ["maturin>=1.4,<2.0"]
build-backend = "maturin"

[project]
name = "goldy"
version = "0.1.0"
description = "Python bindings for the Goldy GPU library"
readme = "PYPI_README.md"
license = { text = "MIT" }
requires-python = ">=3.9"
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Rust",
    "Topic :: Multimedia :: Graphics",
    "Topic :: Scientific/Engineering :: Visualization",
]
keywords = ["gpu", "graphics", "vulkan", "rendering", "compute"]
authors = [
    { name = "Mohamed Koubaa" }
]
dependencies = [
    "numpy>=1.20",
    "glfw==2.8.0",
]

[project.urls]
Homepage = "https://github.com/koubaa/goldy"
Repository = "https://github.com/koubaa/goldy"
Documentation = "https://github.com/koubaa/goldy#readme"

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pillow>=9.0",
]

[tool.maturin]
features = ["pyo3/extension-module"]
python-source = "python"
module-name = "goldy._goldy"

# Include Slang libraries in release wheels (platform-specific).
# CI/maintainers run build-slang.py before `maturin build`; not needed for `pip install -e`.
# Note: paths are relative to python-source directory (python/), not project root
include = [
    { path = "goldy/*.dll", format = "wheel" },
    { path = "goldy/*.so", format = "wheel" },
    { path = "goldy/*.dylib", format = "wheel" },
]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "gpu: tests that require GPU access (may use software renderer in CI)",
]

